#include <stdio.h>
/**
 * add - functions that add two random number together
 * @a: the first random
 * @b: the second random
 * Return: the addition of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub - functions that multiply two random number
 * @a: the first random
 * @b: the second random
 * Return: the sub of a and b
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - functions that sub two random number
 * @a: the first random
 * @b: the second random
 * Return: the mul of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - a functions that divide two random update
 * @a: the first random number
 * @b: the second random number
 * Return: division of a and b
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - a functions that add modules
 * @a: first random
 * @b: second random
 * Return: modules
 */
int mod(int a, int b)
{
	return (a % b);
}
